aurora {
    // Name of the pipeline
    name = "unittest"

    // Trigger Type
    type = "review"

    // CTest tag to run
    ctest_tag = "local"

    // Comamnds to run before test execution
    test_args = "export EXCLUDE_UNSTABLE=1;"

    // Number of test instances to spawn
    num_instances = 4

    // Paths to save logs
    archive = "logs/"

    // Set Voting/Non-Voting Status for pipeline
    voting = "false"
}
